export interface CurrioSubmission {
  id?: string; // ID univoco assegnato da Firebase
  nome: string;
  email: string;
  esperienze: string;
  // Aggiungi qui altri campi se il tuo form ne ha di più
}
